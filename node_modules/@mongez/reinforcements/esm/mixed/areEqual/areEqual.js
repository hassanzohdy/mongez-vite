/**
 * Determine if the given values are equal to each other
 */
function areEqual(value, value2) {
    if (["symbol", "number", "boolean", "string", "undefined"].includes(typeof value) ||
        !value ||
        !value2) {
        return value === value2;
    }
    if (Array.isArray(value)) {
        value = value.sort();
    }
    if (Array.isArray(value2)) {
        value2 = value2.sort();
    }
    return JSON.stringify(value) === JSON.stringify(value2);
}export{areEqual as default};//# sourceMappingURL=areEqual.js.map

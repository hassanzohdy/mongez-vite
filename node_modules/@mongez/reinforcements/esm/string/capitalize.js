import ucfirst from'./ucfirst.js';/**
 * Capitalize the first letter of the string
 *
 * @return string
 */
function capitalize(string) {
    if (!string)
        return "";
    return string
        .split(" ")
        .map(word => ucfirst(word))
        .join(" ");
}export{capitalize as default};//# sourceMappingURL=capitalize.js.map

import toSnakeCase from'./toSnakeCase.js';/**
 * Convert current string to kebab case
 */
function toKebabCase(string, lowerAll = true) {
    if (!string)
        return "";
    return toSnakeCase(string, "-", lowerAll);
}export{toKebabCase as default};//# sourceMappingURL=toKebabCase.js.map

import trim from'./trim.js';const ARABIC_PATTERN = /[\u0600-\u06FF]/;
function startsWithArabic(string, trimmed = true) {
    if (!string)
        return false;
    if (trimmed === true) {
        string = trim(String(string));
    }
    return string.charAt(0).match(ARABIC_PATTERN) !== null;
}export{ARABIC_PATTERN,startsWithArabic as default};//# sourceMappingURL=startsWithArabic.js.map

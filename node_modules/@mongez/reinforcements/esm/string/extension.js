/**
 * Get the extension of the file name
 */
function extension(string) {
    if (!string)
        return "";
    const regex = /(?:\.([^.]+))?$/, extension = regex.exec(string);
    return extension ? extension[1] : "";
}export{extension as default};//# sourceMappingURL=extension.js.map

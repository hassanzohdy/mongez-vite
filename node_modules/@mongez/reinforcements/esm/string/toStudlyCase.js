import capitalize from'./capitalize.js';/**
 * Convert current string to camel case
 * i.e hello-world will become: helloWorld
 *
 * @param string separator default is by dash(-) or underscore(_)
 * @return string
 * @see String.capitalize
 */
function toStudlyCase(string, separator = "-|\\.|_|/|\\s") {
    if (!string)
        return "";
    const regex = new RegExp(separator, "g");
    return string
        .split(regex)
        .map(word => capitalize(word))
        .join("");
}export{toStudlyCase as default};//# sourceMappingURL=toStudlyCase.js.map

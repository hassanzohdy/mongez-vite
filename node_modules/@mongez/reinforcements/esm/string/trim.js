import escapeRegex from'../utils/escapeRegex.js';/**
 * Remove the given needle from the start and the end of string
 *
 * @param string $needle
 * @return string
 */
function trim(string, needle = " ") {
    if (!string)
        return "";
    if (needle === " ") {
        return string.replace(/^\s+|\s+$/g, "");
    }
    needle = escapeRegex(needle);
    const pattern = "^" + needle + "+|" + needle + "+$", regex = new RegExp(pattern, "g");
    return string.replace(regex, "");
}export{trim as default};//# sourceMappingURL=trim.js.map

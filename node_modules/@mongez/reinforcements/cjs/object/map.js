'use strict';/**
 * Map the given object into an array
 */
function map(object, callback) {
    return Object.keys(object).map(key => {
        return callback(key, object[key], object);
    });
}module.exports=map;//# sourceMappingURL=map.js.map

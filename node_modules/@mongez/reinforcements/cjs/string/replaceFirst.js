'use strict';var escapeRegex=require('../utils/escapeRegex.js');/**
 * Replace the First occurrence for the the give needle
 *
 * @param  string needle
 * @param  string replacement
 * @return string
 */
function replaceFirst(string, needle, replacement) {
    if (!string)
        return "";
    return string.replace(escapeRegex(needle), replacement);
}module.exports=replaceFirst;//# sourceMappingURL=replaceFirst.js.map

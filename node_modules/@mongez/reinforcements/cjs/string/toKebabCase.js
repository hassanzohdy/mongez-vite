'use strict';var toSnakeCase=require('./toSnakeCase.js');/**
 * Convert current string to kebab case
 */
function toKebabCase(string, lowerAll = true) {
    if (!string)
        return "";
    return toSnakeCase(string, "-", lowerAll);
}module.exports=toKebabCase;//# sourceMappingURL=toKebabCase.js.map

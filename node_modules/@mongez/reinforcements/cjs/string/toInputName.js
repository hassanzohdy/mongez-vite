'use strict';var rtrim=require('./rtrim.js');/**
 * Convert the current a dot notation string to compound input name
 * my.input.name >> my[input][name]
 */
function toInputName(string) {
    if (!string)
        return "";
    const namesList = string.split(".");
    let mainName = namesList.shift() || "";
    for (let name of namesList) {
        let outBrackets = "";
        if (name.endsWith("[]")) {
            name = rtrim(name, "[]");
            outBrackets = "[]";
        }
        mainName += `[${name}]${outBrackets}`;
    }
    return mainName;
}module.exports=toInputName;//# sourceMappingURL=toInputName.js.map

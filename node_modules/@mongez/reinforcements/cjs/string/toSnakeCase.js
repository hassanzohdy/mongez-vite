'use strict';/**
 * Convert current string to snake case
 * i.e hello-world will become: hello_world
 *
 * @return string
 */
function toSnakeCase(string, separator = "_", lowerAll = true) {
    if (!string)
        return "";
    return string
        .replace(/(-|\/|\s|([A-Z]))+/g, function (_match, _v2, matchedUpperLetter) {
        if (!matchedUpperLetter)
            return separator;
        return (separator +
            (lowerAll ? matchedUpperLetter.toLowerCase() : matchedUpperLetter));
    })
        .replace(new RegExp(`^${separator}`), "");
}module.exports=toSnakeCase;//# sourceMappingURL=toSnakeCase.js.map

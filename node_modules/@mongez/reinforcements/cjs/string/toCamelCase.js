'use strict';var capitalize=require('./capitalize.js');function toCamelCase(string, separator = "\\s+|-|/|_|\\.") {
    if (!string)
        return "";
    const regex = new RegExp(separator + "|(?=[A-Z])", "g");
    return string
        .split(regex)
        .map((word, index) => (index > 0 ? capitalize(word) : word.toLowerCase()))
        .join("");
}module.exports=toCamelCase;//# sourceMappingURL=toCamelCase.js.map

'use strict';var ucfirst=require('./ucfirst.js');/**
 * Capitalize the first letter of the string
 *
 * @return string
 */
function capitalize(string) {
    if (!string)
        return "";
    return string
        .split(" ")
        .map(word => ucfirst(word))
        .join(" ");
}module.exports=capitalize;//# sourceMappingURL=capitalize.js.map
